1. 创建 `app` 目录
2. 创建 `app/database.py` 文件：

   * 导入必要的 SQLAlchemy 模块

   * 创建 MySQL、SQL Server、Oracle 三个独立的 Engine

   * 编写 `get_all_sessions()` 生成器函数
3. 创建 `app/models.py` 文件：

   * 导入必要的 SQLAlchemy 模块

   * 定义 Base 类

   * 定义 User、Question、Exam、Score、SyncLog 五个模型类
4. 创建 `app/init_db.py` 文件：

   * 导入必要的模块

   * 获取所有数据库连接

   * 在三个数据库中同时创建表
5. 创建 `app/main.py` 文件：

   * 导入必要的模块

   * 创建 FastAPI 应用

   * 实现 `/test-connection` 接口
6. 创建 `requirements.txt` 文件，列出所有依赖项

